# ── Cliente (Vite) — se exponen al navegador ──────────────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── Servidor (funciones /api/admin/*) — NUNCA con prefijo VITE_ ────────────
# Se configuran en el panel de Vercel (Environment Variables), no en el bundle.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
